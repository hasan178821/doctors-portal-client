import React from 'react';

const AppointmentsByDateDetails = ({bookDate}) => {
    return (
        <div>
            <h1>Appointment{bookDate.length}</h1>
        </div>
    );
};

export default AppointmentsByDateDetails;